🧠 目的
抽象化と関数の再利用性の練習

問題内容
📝 要件：
スライスと関数を渡すことで条件を満たす要素のみを返すfilter関数を実装

✅ 制約＆補足
関数名：filter(nums []int, f func(int) bool) []int
例として、偶数だけを取り出すなどに使えるように

🔄 実行例
filter([]int{1,2,3,4}, func(n int) bool { return n%2 == 0 }) // [2,4]